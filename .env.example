# Protoagent - Example environment variables (only those used by the code)

# ------------------
# Telegram (required)
# ------------------
# Telegram bot token (obtained via BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here
# Authorized user IDs (comma-separated). Only the first will be used.
ALLOWED_USER_IDS=123456789

# ------------------
# AI Provider (optional)
# ------------------
# AI provider to use: 'claude' or 'copilot' (default: 'claude')
AI_PROVIDER=claude

# ------------------
# Whisper (audio) — minimal options
# ------------------
# Transcription model (default: base)
WHISPER_MODEL=base
# Audio language ('auto' for automatic detection)
WHISPER_LANGUAGE=auto

# ------------------
# Application
# ------------------
# Log level: error, warn, info, debug
LOG_LEVEL=info
# How many consecutive crashes before halting (circuit breaker)
MAX_CRASHES=3
